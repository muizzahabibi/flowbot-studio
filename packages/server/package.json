{
  "name": "@google-flow-suite/server",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsup src/main.ts --format esm --dts",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@google-flow-suite/core": "workspace:*",
    "dotenv-safe": "^9.1.0",
    "fastify": "^5.2.1"
  }
}
