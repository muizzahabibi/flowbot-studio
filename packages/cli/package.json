{
  "name": "@google-flow-suite/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "flow": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@google-flow-suite/core": "workspace:*",
    "dotenv-safe": "^9.1.0"
  }
}
